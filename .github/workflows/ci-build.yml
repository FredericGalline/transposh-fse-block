name: 🧱 WordPress Build – Blocs Gutenberg

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-blocks:
    name: 🔨 Build des blocs Gutenberg
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Cloner le dépôt
        uses: actions/checkout@v3

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📚 Installer les dépendances (avec cache)
        run: npm ci || npm install
        
      - name: 🧶 Builder tous les blocs
        run: npm run build
